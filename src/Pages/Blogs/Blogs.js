import React from 'react';
import useTitle from '../../Hooks/Title/useTitle';

const Blogs = () => {
    useTitle('blogs page')
    return (
        <div className='h-screen'>
            <h4>Blogs Comming Soon..........</h4>
        </div>
    );
};

export default Blogs;